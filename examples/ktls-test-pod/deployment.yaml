apiVersion: v1
kind: Namespace
metadata:
  name: ktls-test
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ktls-test
  namespace: ktls-test
  labels:
    app: ktls-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ktls-test
  template:
    metadata:
      labels:
        app: ktls-test
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
        hostUsers: false
        fsGroup: 0
      containers:
        - name: server
          image: __KTLS_TEST_IMAGE__
          imagePullPolicy: __IMAGE_PULL_POLICY__
          ports:
            - name: https
              containerPort: 8443
          readinessProbe:
            httpGet:
              path: /healthz
              port: https
              scheme: HTTPS
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: https
              scheme: HTTPS
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities:
              drop: ["ALL"]
---
apiVersion: v1
kind: Service
metadata:
  name: ktls-test
  namespace: ktls-test
spec:
  selector:
    app: ktls-test
  ports:
    - name: https
      port: 8443
      targetPort: https
